{{#each cities as |city|}}
  <a href="/cities/{{city.id}}/edit">{{city.name}}</a>
  <form action="/cities/{{city.id}}/delete" method="POST">
    <button type="submit" class="btn btn-primary">Delete</button>
  </form>
{{/each}}